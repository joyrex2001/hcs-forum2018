def template_kafka = "${TEMPLATE_KAFKA}"
def template_database = "${TEMPLATE_DATABASE}"
def projectbase = "${PROJECTBASE}"
def buildproject = "${projectbase}-dev"
def prodproject = "${projectbase}-prod"

pipeline {
  agent {
    node {
      label 'maven'
    }
  }
  stages {
    stage("Update kafka and postgresql in dev project") {
      steps {
        script {
          openshift.withCluster() {
            openshift.withProject("${buildproject}") {
              [template_kafka, template_database].each { openshift.apply(openshift.process("-f", it)) }
            }
          }
        }
      }
    }
    stage("Ask for approval") {
      steps {
        script {
          openshift.withCluster() {
            input("Deploy to Production?")
          }
        }
      }
    }
    stage("Update kafka and postgresql in prod project") {
      steps {
        script {
          openshift.withCluster() {
            openshift.withProject("${prodproject}") {
              [template_kafka, template_database].each { openshift.apply(openshift.process("-f", it)) }
            }
          }
        }
      }
    }
  }
}
